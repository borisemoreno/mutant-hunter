runtime: java
env: flex
runtime_config:
  jdk: openjdk8
env_variables:
  SPRING_PROFILES_ACTIVE: "gcp,postgresql"
handlers:
  - url: /.*
    script: this field is required, but ignored
resources:
  memory_gb: 1
manual_scaling:
  instances: 1